# Local Ollama models

Embeddings are generated locally with the `nomic-embed-text` model through Ollama's `/api/embed` endpoint. Grounded answer generation uses a separately configured local Ollama generation model.
