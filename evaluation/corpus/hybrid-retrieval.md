# Hybrid retrieval

Reciprocal Rank Fusion (RRF) combines dense and sparse ranking positions. It does not add raw cosine and BM25 scores because their scales are incomparable. Hybrid retrieval exposes component ranks and scores for inspection.
